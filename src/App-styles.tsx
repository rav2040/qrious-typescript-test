import styled from 'styled-components';

const Container = styled.div`
  margin: 2rem;
  text-align: center;

  h1 {
    margin: 0;
    margin-left: 2rem;
    text-align: left;
  }
`;

export const div = {
  Container,
};
